define([
        'backbone',
    ],
    function(Backbone) {
        'use strict';

        var link = Backbone.Model.extend({
            initialize: function() {
            }
        });

        return link;
    });
